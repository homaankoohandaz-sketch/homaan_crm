# Phase 01 — CRM Core & Identity

Canonical scope: identity, permissions, people, owners, buyers, investors, builders, suppliers, properties, files, requests, leads, phones, import, normalization and audit.

Primary code: `src/domains/crm/`, `src/core/data/`, current `index.html` + `buildwise-app.js`, Supabase auth/RLS.

Do not add new CRM logic to legacy duplicate index files.
