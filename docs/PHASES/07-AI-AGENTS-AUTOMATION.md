# Phase 07 — AI / Agents / Automation

Canonical scope: AI gateway, section assistants, tool registry, task contracts, model routing, worker registry, handoffs, memory, audit, approval and recovery.

Primary code: `.agent-control/`, `src/ai/`, `src/agent-control/`, Supabase `ai-orchestrator`.
