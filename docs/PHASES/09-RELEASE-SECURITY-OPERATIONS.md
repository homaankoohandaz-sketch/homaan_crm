# Phase 09 — Release / Security / Operations

Canonical scope: tests, CI, RLS, permissions, secret management, deployment, observability, backup, rollback and environment separation.

Primary code: `tests/`, `.github/`, `supabase/`, `netlify.toml`, `.agent-control/` release gates.
