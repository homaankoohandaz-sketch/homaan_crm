# Phase 06 — Customer / Builder Portals & Showroom

Canonical scope: customer room, builder workspace, project presentation, media, floor/unit views, requests, shareable project outputs.

Primary code: customer portal Edge Function, `visual-project.html`, portal UI and media registry. Migrate legacy presentation pages only with verification.
