# Tasks

Tasks are small, independently claimable work units.

Required fields:
- status
- owner
- priority
- objective
- acceptance criteria

Agents must not silently rewrite another agent's active task.
