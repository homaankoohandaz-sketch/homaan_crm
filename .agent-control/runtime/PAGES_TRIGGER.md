# Public deployment verification

This marker intentionally triggers the GitHub Pages deployment workflow after the control-plane deployment changes.

Done-when: the Pages workflow completes successfully and its published URL returns the CRM entrypoint.
